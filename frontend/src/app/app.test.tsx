import { describe, it, expect } from 'vitest'

// 프론트엔드 프로젝트가 정상적으로 설정되었는지 확인하는 테스트
describe('프론트엔드 프로젝트 초기화', () => {
  it('shouldBuildSuccessfully', () => {
    // vitest가 정상 실행되면 프로젝트 초기화 성공
    expect(true).toBe(true)
  })
})
